#include "MySBBST.hh"
#include "student.h"

bool intintCompare::lt(int x, int y)
{
  return x < y;
}

bool intintCompare::gt(int x, int y)
{
	return y < x;
}

bool intintCompare::eq(int x, int y)
{
	return x == y;
}

