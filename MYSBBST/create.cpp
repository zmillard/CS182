#include "MySBBST.hh"
/**
 * 
 * 
 * @author    Zoe Millard
 * @version   1.1
 * @since     20150227
 *
 */
/*
template void MySBBST<int, intintCompare>::MySBBSTNode(int*);
template void MySBBST<int, intintCompare>::MySBBSTNode();

template <class Elem, class Comp, class Key, class EEComp, class KEComp>
/*
 * Uses the selection algorithm to sort the elements of the array 
 * 
 * requires  - arr != null && 0 <= n < the length of arr
 * ensures   - contents of the array are sorted
 *
 * @param   arr   The array of elements to be sorted
 * @param   n   The size of the data to be sorted
 * 
 *
 */
 /*
 MySBBSTNode *lc;
 MySBBSTNode *rc;
 Elem e;
 int color; //red is 1, black is 0
 
void MySBBST<Elem, Comp, Key, EEComp, KEComp>::MySBBSTNode(Elem a) 
{
	lc = NULL;
	rc = NULL;
	e = a;
	color = 1;
	
}
void MySBBST<Elem, Comp, Key, EEComp, KEComp>::MySBBSTNode() 
{
	lc = NULL;
	rc = NULL;
	e = 0;
	color = 0;
}
*/