#include "SBBST.hh"
/*
template void Sortings<int, intintCompare>::swap(int*, unsigned int, unsigned int);

template <class Elem, class Comp>
void MySBBST<Elem, Comp>::swap(Elem* arr, unsigned int x, unsigned int y)
{
	Elem temp = arr[x];
	arr[x] = arr[y];
	arr[y] = temp;
}
/*